Board-sync lifecycle end-to-end: the pure setProjectStatus() helper driven
against a simulated Projects v2 board (mock GraphQL executor, no network) for
issue #26. Shows the card actually moving columns as the agent loop fires each
lifecycle event - matching how the workflows call it (In Progress on claim,
In Review on PR/respond, Needs captain on the park path). "Done" is intentionally
left to GitHub's native merge->Done workflow.

Card for issue #26 starts in column: "Todo"

Lifecycle event: ticket CLAIMED (agent-dispatch) -> set In Progress
  set-project-status: set "Tic-Tac-Toe Roadmap" Status to "In Progress" for issue #26.
  result: {"updated":1}
  board column is now: "In Progress"

Lifecycle event: PR OPENED / agent responded (agent-respond) -> set In Review
  set-project-status: set "Tic-Tac-Toe Roadmap" Status to "In Review" for issue #26.
  result: {"updated":1}
  board column is now: "In Review"

Lifecycle event: PARKED, no PR (agent-dispatch park path) -> set Needs captain
  set-project-status: set "Tic-Tac-Toe Roadmap" Status to "Needs captain" for issue #26.
  result: {"updated":1}
  board column is now: "Needs captain"
